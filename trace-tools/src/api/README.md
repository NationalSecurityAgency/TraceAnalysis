Initial testing around using a SQL database for trace analysis workloads as well as an API library that serves as the unified frontend to both the database and spacetime/string indices (which should simplify the ghidra, gdb, and other frontends for exploring the computed data).


